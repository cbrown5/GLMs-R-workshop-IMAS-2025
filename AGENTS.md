
# R software

Use R software for all coding tasks

Use `tidyverse` packages. 

Use `theme_classic()` for all ggplots. 

Use .qmd format to document results and figures. 
When using quarto set `code-fold: show` in the YAML